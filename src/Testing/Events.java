package Testing;

import org.bukkit.event.Listener;

public class Events implements Listener {
	/**@EventHandler
	public void onMove(EntityMoveEvent e) { <-- Working
		TheAPI.broadcastMessage(e.getEntityType().name());
	}**/
}
